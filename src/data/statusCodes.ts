import type { StatusCode } from '../types/status';

export const statusCodes: StatusCode[] = [
  // 1xx Informational
  {
    code: 100,
    name: 'Continue',
    category: 'informational',
    description: 'The server has received the request headers and the client should proceed to send the request body.',
    whenToUse: ['When handling large payloads (like file uploads) and the client asks for confirmation with the "Expect: 100-continue" header.'],
    whenNotToUse: ['Normal API responses where the request is small or already completed.', 'Application-level business logic.'],
    exampleScenario: 'Client uploading a 5GB video file and asking if the headers are acceptable before streaming the body.',
    developerMessage: 'Headers received, proceed with body transmission.',
    productionMessage: '',
    hasBody: false,
    common: false,
  },
  {
    code: 101,
    name: 'Switching Protocols',
    category: 'informational',
    description: 'The requester has asked the server to switch protocols and the server has agreed to do so.',
    whenToUse: ['When upgrading an HTTP connection to a WebSocket connection.'],
    whenNotToUse: ['Standard REST API endpoints.'],
    exampleScenario: 'Client sends an "Upgrade: websocket" header to establish a persistent real-time connection.',
    developerMessage: 'Protocol upgraded to WebSocket.',
    productionMessage: '',
    hasBody: false,
    common: true,
  },
  {
    code: 102,
    name: 'Processing',
    category: 'informational',
    description: 'The server has received and is processing the request, but no response is available yet (WebDAV).',
    whenToUse: ['Rarely, mostly in WebDAV when processing takes a long time.'],
    whenNotToUse: ['Modern APIs. Use 202 Accepted instead for async processing.'],
    exampleScenario: 'A complex WebDAV file operation is taking longer than expected.',
    developerMessage: 'Processing request (WebDAV).',
    productionMessage: '',
    hasBody: false,
    common: false,
    deprecated: true,
  },
  {
    code: 103,
    name: 'Early Hints',
    category: 'informational',
    description: 'Used to return some response headers before final HTTP message. Allows browser to start preloading resources.',
    whenToUse: ['When the server needs time to generate a full response, but knows which static assets (CSS/JS) the client will need.'],
    whenNotToUse: ['For API responses where no static assets are involved.'],
    exampleScenario: 'Preloading a large CSS bundle while the server renders the HTML.',
    developerMessage: 'Early hints for resource preloading.',
    productionMessage: '',
    hasBody: false,
    common: false,
  },

  // 2xx Success
  {
    code: 200,
    name: 'OK',
    category: 'success',
    description: 'The request succeeded. The response body contains the requested data.',
    whenToUse: ['When fetching data (GET).', 'When updating data and returning the updated resource (PUT/PATCH).'],
    whenNotToUse: ['When creating a new resource (use 201).', 'When there is no response body (use 204).', 'When returning an error (use 4xx/5xx).'],
    exampleScenario: 'Fetching user details via GET /users/123.',
    developerMessage: 'User fetched successfully.',
    productionMessage: 'Details loaded successfully.',
    responseExample: '{\n  "id": "123",\n  "name": "Nikhil"\n}',
    hasBody: true,
    common: true,
    relatedCodes: [201, 204],
  },
  {
    code: 201,
    name: 'Created',
    category: 'success',
    description: 'The request succeeded, and a new resource was created as a result.',
    whenToUse: ['After a successful POST request that creates a new record in a database.'],
    whenNotToUse: ['When no new resource was created (use 200).', 'When the creation happens asynchronously (use 202).'],
    exampleScenario: 'A user successfully signs up and their profile is created.',
    developerMessage: 'User resource created with ID 456.',
    productionMessage: 'Account created successfully.',
    responseExample: '{\n  "id": "456",\n  "status": "active"\n}',
    hasBody: true,
    common: true,
    relatedCodes: [200, 202],
  },
  {
    code: 202,
    name: 'Accepted',
    category: 'success',
    description: 'The request has been received but not yet acted upon. It is intended for cases where another process or server handles the request.',
    whenToUse: ['When placing a job in a background queue.', 'For long-running asynchronous processes.'],
    whenNotToUse: ['When the process finishes immediately before the HTTP response is sent.'],
    exampleScenario: 'User requests a heavy report generation that takes 5 minutes. The API returns 202 and a job ID.',
    developerMessage: 'Report generation job queued (JobID: 9876).',
    productionMessage: 'Your report is being generated.',
    responseExample: '{\n  "jobId": "9876",\n  "status": "pending",\n  "pollUrl": "/jobs/9876"\n}',
    hasBody: true,
    common: true,
    relatedCodes: [200, 201],
  },
  {
    code: 203,
    name: 'Non-Authoritative Information',
    category: 'success',
    description: 'The request was successful but the enclosed payload has been modified by a transforming proxy.',
    whenToUse: ['Very rarely in modern APIs. Mostly handled by proxy infrastructure.'],
    whenNotToUse: ['Normal application code.'],
    exampleScenario: 'A proxy server modifies headers or the body of a 200 OK response before sending it to the client.',
    developerMessage: 'Response modified by proxy.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 204,
    name: 'No Content',
    category: 'success',
    description: 'The server successfully fulfilled the request and there is no additional content to send in the response payload body.',
    whenToUse: ['After a successful DELETE request.', 'After a successful PUT/PATCH when no updated object needs to be returned.'],
    whenNotToUse: ['When the client needs updated data back.', 'When there is an error.'],
    exampleScenario: 'A user deletes an item from their cart.',
    developerMessage: 'Resource deleted. No content to return.',
    productionMessage: 'Item removed.',
    hasBody: false,
    common: true,
    relatedCodes: [200],
  },
  {
    code: 205,
    name: 'Reset Content',
    category: 'success',
    description: 'The server successfully processed the request, asks that the requester reset its document view, and is not returning any content.',
    whenToUse: ['When asking a web browser to clear a form after successful submission.'],
    whenNotToUse: ['In JSON/REST APIs (clients usually handle form clearing).'],
    exampleScenario: 'Submitting a contact form where the server wants the browser to blank it out.',
    developerMessage: 'Content reset requested.',
    productionMessage: '',
    hasBody: false,
    common: false,
  },
  {
    code: 206,
    name: 'Partial Content',
    category: 'success',
    description: 'The server is delivering only part of the resource due to a range header sent by the client.',
    whenToUse: ['When streaming large files (video/audio).', 'When the client uses the "Range" header to resume a download.'],
    whenNotToUse: ['For normal JSON API pagination (use 200 with pagination metadata).'],
    exampleScenario: 'A browser requests bytes 500-999 of a video file.',
    developerMessage: 'Returning bytes 500-999 of the requested file.',
    productionMessage: '',
    hasBody: true,
    common: true,
  },
  {
    code: 207,
    name: 'Multi-Status',
    category: 'success',
    description: 'Conveys information about multiple resources, for situations where multiple status codes might be appropriate (WebDAV).',
    whenToUse: ['WebDAV or specialized batch operations where partial successes are common.'],
    whenNotToUse: ['Standard REST APIs (usually prefer 200 with a custom JSON structure or atomic operations).'],
    exampleScenario: 'Moving 5 files where 3 succeeded, 1 was forbidden, and 1 was not found.',
    developerMessage: 'Batch operation complete with mixed results.',
    productionMessage: 'Some operations succeeded.',
    hasBody: true,
    common: false,
  },
  {
    code: 208,
    name: 'Already Reported',
    category: 'success',
    description: 'Used inside a DAV: propstat response element to avoid enumerating the internal members of multiple bindings to the same collection repeatedly.',
    whenToUse: ['Only in WebDAV.'],
    whenNotToUse: ['Any normal API.'],
    exampleScenario: 'WebDAV directory traversal optimization.',
    developerMessage: 'Already reported in previous response.',
    productionMessage: '',
    hasBody: false,
    common: false,
  },
  {
    code: 226,
    name: 'IM Used',
    category: 'success',
    description: 'The server has fulfilled a GET request for the resource, and the response is a representation of the result of one or more instance-manipulations applied to the current instance.',
    whenToUse: ['Delta encoding in HTTP.'],
    whenNotToUse: ['Standard APIs.'],
    exampleScenario: 'Returning a diff of a document instead of the whole document.',
    developerMessage: 'Delta applied.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },

  // 3xx Redirection
  {
    code: 300,
    name: 'Multiple Choices',
    category: 'redirection',
    description: 'Indicates multiple options for the resource from which the client may choose.',
    whenToUse: ['When content negotiation (like choosing a video format or language) cannot be resolved automatically.'],
    whenNotToUse: ['When a single optimal representation exists.'],
    exampleScenario: 'A URL points to a video available in both MP4 and WebM formats.',
    developerMessage: 'Multiple representations available. Please choose one.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 301,
    name: 'Moved Permanently',
    category: 'redirection',
    description: 'The URL of the requested resource has been changed permanently. The new URL is given in the response.',
    whenToUse: ['When restructuring a website or migrating to a new domain.', 'To enforce HTTPS over HTTP.'],
    whenNotToUse: ['For temporary redirects (use 302 or 307).', 'When preserving POST data is critical (use 308).'],
    exampleScenario: 'A user visits http://example.com and is redirected to https://example.com.',
    developerMessage: 'Resource permanently moved to a new URL.',
    productionMessage: 'Redirecting...',
    hasBody: false,
    common: true,
    relatedCodes: [308, 302],
  },
  {
    code: 302,
    name: 'Found',
    category: 'redirection',
    description: 'The URL of the requested resource has been changed temporarily. Historically, many clients change POST to GET upon receiving this.',
    whenToUse: ['When temporarily redirecting a user (e.g., to a login page).'],
    whenNotToUse: ['For permanent redirects (use 301).', 'When the HTTP method must strictly remain a POST (use 307).'],
    exampleScenario: 'An unauthenticated user tries to view a dashboard and is redirected to /login.',
    developerMessage: 'Temporarily redirecting to /login.',
    productionMessage: 'Redirecting to login...',
    hasBody: false,
    common: true,
    relatedCodes: [301, 303, 307],
  },
  {
    code: 303,
    name: 'See Other',
    category: 'redirection',
    description: 'Directs the client to get the requested resource at another URI with a GET request.',
    whenToUse: ['Following a successful POST (Post/Redirect/Get pattern) to prevent double form submissions.'],
    whenNotToUse: ['When the client should preserve the original HTTP method (use 307).'],
    exampleScenario: 'User submits a payment form (POST) and is redirected to a receipt page (GET).',
    developerMessage: 'Action successful, redirecting to receipt using GET.',
    productionMessage: 'Processing...',
    hasBody: false,
    common: true,
  },
  {
    code: 304,
    name: 'Not Modified',
    category: 'redirection',
    description: 'Tells the client that the requested resource has not been modified since the last time they accessed it, so they can use their cached version.',
    whenToUse: ['When a client sends an "If-None-Match" (ETag) or "If-Modified-Since" header and the resource hasn\'t changed.'],
    whenNotToUse: ['When the resource has changed (return 200).', 'When the client doesn\'t use conditional caching headers.'],
    exampleScenario: 'Browser requests an image it already has in its cache, and the server confirms it is up to date.',
    developerMessage: 'Resource not modified (ETag matches). Use cached version.',
    productionMessage: '',
    hasBody: false,
    common: true,
  },
  {
    code: 305,
    name: 'Use Proxy',
    category: 'redirection',
    description: 'Defined in a previous version of the HTTP specification to indicate that a requested response must be accessed by a proxy. It has been deprecated due to security concerns.',
    whenToUse: ['Never.'],
    whenNotToUse: ['Always.'],
    exampleScenario: 'Deprecated.',
    developerMessage: 'Deprecated.',
    productionMessage: '',
    hasBody: false,
    common: false,
    deprecated: true,
  },
  {
    code: 306,
    name: 'Switch Proxy',
    category: 'redirection',
    description: 'No longer used. Originally meant "Subsequent requests should use the specified proxy."',
    whenToUse: ['Never.'],
    whenNotToUse: ['Always.'],
    exampleScenario: 'Deprecated.',
    developerMessage: 'Deprecated.',
    productionMessage: '',
    hasBody: false,
    common: false,
    deprecated: true,
  },
  {
    code: 307,
    name: 'Temporary Redirect',
    category: 'redirection',
    description: 'The server sends this response to direct the client to get the requested resource at another URI with the same method that was used in the prior request.',
    whenToUse: ['When a resource is temporarily moved and you want a POST request to remain a POST request on the new URL.'],
    whenNotToUse: ['When you want a POST to turn into a GET (use 303).', 'For permanent moves (use 308).'],
    exampleScenario: 'An API endpoint temporarily changes URL during maintenance, and incoming POST requests must be preserved.',
    developerMessage: 'Temporary redirect. Client must retain original HTTP method.',
    productionMessage: '',
    hasBody: false,
    common: true,
    relatedCodes: [302, 308],
  },
  {
    code: 308,
    name: 'Permanent Redirect',
    category: 'redirection',
    description: 'The resource is now permanently located at another URI. The client MUST use the same method for the new request as was used for the original request.',
    whenToUse: ['When an API endpoint permanently moves, and POST requests must remain POST requests.'],
    whenNotToUse: ['When you want a POST to turn into a GET.', 'For temporary moves (use 307).'],
    exampleScenario: 'An API moves from /v1/users to /v2/users and requires clients to resend their POST payload to the new URL.',
    developerMessage: 'Permanent redirect. Client must retain original HTTP method.',
    productionMessage: '',
    hasBody: false,
    common: true,
    relatedCodes: [301, 307],
  },

  // 4xx Client Errors
  {
    code: 400,
    name: 'Bad Request',
    category: 'client-error',
    description: 'The server cannot or will not process the request due to something that is perceived to be a client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing).',
    whenToUse: ['When the request JSON is malformed.', 'When a required query parameter is missing.', 'General validation errors (though 422 is better for semantic validation).'],
    whenNotToUse: ['When the request is syntactically correct but fails business logic validation (use 422).', 'Authentication failures (use 401).'],
    exampleScenario: 'Sending invalid JSON syntax to an API.',
    developerMessage: 'Syntax error in JSON body at line 4.',
    productionMessage: 'The provided information is invalid.',
    responseExample: '{\n  "success": false,\n  "message": "Invalid JSON syntax."\n}',
    hasBody: true,
    common: true,
    relatedCodes: [422],
  },
  {
    code: 401,
    name: 'Unauthorized',
    category: 'client-error',
    description: 'Although the HTTP standard specifies "unauthorized", semantically this response means "unauthenticated". That is, the client must authenticate itself to get the requested response.',
    whenToUse: ['When a user is not logged in.', 'When a JWT token is expired, missing, or invalid.'],
    whenNotToUse: ['When a user is logged in but lacks permissions (use 403).'],
    exampleScenario: 'Trying to access a private dashboard without an Authorization header.',
    developerMessage: 'Missing Authorization header. Token required.',
    productionMessage: 'Please log in to access this resource.',
    hasBody: true,
    common: true,
    relatedCodes: [403],
  },
  {
    code: 402,
    name: 'Payment Required',
    category: 'client-error',
    description: 'This response code is reserved for future use. The initial aim for creating this code was using it for digital payment systems, however this code is not used widely.',
    whenToUse: ['Rarely, but sometimes used in SaaS APIs to indicate an account has run out of credits or has billing issues.'],
    whenNotToUse: ['General application errors.'],
    exampleScenario: 'An API request fails because the user\'s subscription has expired.',
    developerMessage: 'Account billing issue. Credits exhausted.',
    productionMessage: 'Please update your billing information.',
    hasBody: true,
    common: false,
  },
  {
    code: 403,
    name: 'Forbidden',
    category: 'client-error',
    description: 'The client does not have access rights to the content; that is, it is unauthorized, so the server is refusing to give the requested resource. Unlike 401, the client\'s identity is known to the server.',
    whenToUse: ['When a logged-in user tries to access a resource they don\'t own.', 'When an admin-only endpoint is accessed by a regular user.'],
    whenNotToUse: ['When the user is not logged in at all (use 401).'],
    exampleScenario: 'User A tries to delete a post created by User B.',
    developerMessage: 'User does not have the "delete:post" permission for this resource.',
    productionMessage: 'You do not have permission to perform this action.',
    hasBody: true,
    common: true,
    relatedCodes: [401, 404],
  },
  {
    code: 404,
    name: 'Not Found',
    category: 'client-error',
    description: 'The server can not find the requested resource. In the browser, this means the URL is not recognized. In an API, this can also mean that the endpoint is valid but the resource itself does not exist.',
    whenToUse: ['When fetching a resource by ID that doesn\'t exist.', 'When an endpoint URL is misspelled.', 'To hide the existence of a resource from unauthorized users (instead of 403).'],
    whenNotToUse: ['When the endpoint exists but the HTTP method is wrong (use 405).', 'When the resource was intentionally deleted and you want to communicate that (use 410).'],
    exampleScenario: 'GET /users/999 where user 999 is not in the database.',
    developerMessage: 'Resource User#999 not found in database.',
    productionMessage: 'The requested resource could not be found.',
    hasBody: true,
    common: true,
    relatedCodes: [403, 410],
  },
  {
    code: 405,
    name: 'Method Not Allowed',
    category: 'client-error',
    description: 'The request method is known by the server but is not supported by the target resource.',
    whenToUse: ['When an endpoint supports GET but a client sends a POST request to it.'],
    whenNotToUse: ['When the endpoint doesn\'t exist at all (use 404).'],
    exampleScenario: 'Sending a POST request to a read-only endpoint.',
    developerMessage: 'Method POST not supported for this route. Allowed: GET, OPTIONS.',
    productionMessage: 'This action is not supported here.',
    hasBody: true,
    common: true,
  },
  {
    code: 406,
    name: 'Not Acceptable',
    category: 'client-error',
    description: 'This response is sent when the web server, after performing server-driven content negotiation, doesn\'t find any content that conforms to the criteria given by the user agent.',
    whenToUse: ['When a client sends an "Accept" header for a format the API doesn\'t support (e.g. Accept: application/xml, but the API only speaks JSON).'],
    whenNotToUse: ['General errors.'],
    exampleScenario: 'Client requests XML from a JSON-only API.',
    developerMessage: 'Requested Content-Type application/xml not supported. Supported: application/json.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 407,
    name: 'Proxy Authentication Required',
    category: 'client-error',
    description: 'This is similar to 401 Unauthorized but authentication is needed to be done by a proxy.',
    whenToUse: ['When working with authenticated proxies.'],
    whenNotToUse: ['Application-level authentication (use 401).'],
    exampleScenario: 'A corporate network proxy requires a username/password before letting the request out to the internet.',
    developerMessage: 'Proxy authentication required.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 408,
    name: 'Request Timeout',
    category: 'client-error',
    description: 'This response is sent on an idle connection by some servers, even without any previous request by the client. It means that the server would like to shut down this unused connection.',
    whenToUse: ['When the client opens a connection but takes too long to send the request payload.'],
    whenNotToUse: ['When the server takes too long to process a request (use 504 Gateway Timeout).'],
    exampleScenario: 'A client starts uploading a file on a slow connection but stops sending data.',
    developerMessage: 'Client failed to send request in the allotted time.',
    productionMessage: 'Request timed out.',
    hasBody: true,
    common: false,
  },
  {
    code: 409,
    name: 'Conflict',
    category: 'client-error',
    description: 'This response is sent when a request conflicts with the current state of the server.',
    whenToUse: ['When creating a user with an email address that already exists.', 'When trying to edit a resource that was modified by someone else (optimistic concurrency).'],
    whenNotToUse: ['For general validation errors (use 422).'],
    exampleScenario: 'Registering a new account with an email that is already registered.',
    developerMessage: 'Unique constraint violation on column "email".',
    productionMessage: 'An account with this email already exists.',
    hasBody: true,
    common: true,
    relatedCodes: [422],
  },
  {
    code: 410,
    name: 'Gone',
    category: 'client-error',
    description: 'This response is sent when the requested content has been permanently deleted from server, with no forwarding address.',
    whenToUse: ['When an API version is deprecated and removed.', 'When a resource was intentionally deleted and you want clients to stop asking for it.'],
    whenNotToUse: ['When a resource was never there (use 404).', 'When the deletion is temporary.'],
    exampleScenario: 'A client requests a promotional page for a marketing campaign that ended years ago.',
    developerMessage: 'Resource permanently deleted on 2024-01-01.',
    productionMessage: 'This resource is no longer available.',
    hasBody: true,
    common: true,
    relatedCodes: [404],
  },
  {
    code: 411,
    name: 'Length Required',
    category: 'client-error',
    description: 'Server rejected the request because the Content-Length header field is not defined and the server requires it.',
    whenToUse: ['When an API strictly requires the client to declare payload size before accepting it.'],
    whenNotToUse: ['Generally handled by web servers/frameworks automatically.'],
    exampleScenario: 'Sending a POST request without a Content-Length header.',
    developerMessage: 'Content-Length header is missing.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 412,
    name: 'Precondition Failed',
    category: 'client-error',
    description: 'The client has indicated preconditions in its headers which the server does not meet.',
    whenToUse: ['When implementing optimistic locking using If-Match or If-Unmodified-Since headers and the condition fails.'],
    whenNotToUse: ['For business logic rules (use 422).'],
    exampleScenario: 'Client tries to update a document with If-Match: "v1", but the server document is already at "v2".',
    developerMessage: 'ETag mismatch on If-Match header.',
    productionMessage: 'The document was updated by someone else.',
    hasBody: true,
    common: true,
  },
  {
    code: 413,
    name: 'Content Too Large',
    category: 'client-error',
    description: 'Request entity is larger than limits defined by server. The server might close the connection or return an Retry-After header field.',
    whenToUse: ['When a user uploads a file that exceeds the maximum allowed size.'],
    whenNotToUse: ['When the JSON string is too long for a database column (usually 400 or 422 depending on context).'],
    exampleScenario: 'Uploading a 50MB image when the limit is 5MB.',
    developerMessage: 'Payload exceeds 5MB limit.',
    productionMessage: 'The file you are trying to upload is too large.',
    hasBody: true,
    common: true,
  },
  {
    code: 414,
    name: 'URI Too Long',
    category: 'client-error',
    description: 'The URI requested by the client is longer than the server is willing to interpret.',
    whenToUse: ['When a GET request has too many query parameters and exceeds server limits.'],
    whenNotToUse: ['For large request bodies (use 413).'],
    exampleScenario: 'A search request with thousands of filter IDs in the query string.',
    developerMessage: 'URI length exceeds maximum allowed limit.',
    productionMessage: 'The request is too complex.',
    hasBody: true,
    common: false,
  },
  {
    code: 415,
    name: 'Unsupported Media Type',
    category: 'client-error',
    description: 'The media format of the requested data is not supported by the server, so the server is rejecting the request.',
    whenToUse: ['When a client sends XML but the server only accepts JSON (checked via Content-Type header).'],
    whenNotToUse: ['When the client requests a format the server can\'t output (use 406).'],
    exampleScenario: 'POSTing a file with Content-Type: text/plain to an endpoint that requires application/json.',
    developerMessage: 'Content-Type text/plain not supported. Please send application/json.',
    productionMessage: 'Invalid data format submitted.',
    hasBody: true,
    common: true,
  },
  {
    code: 416,
    name: 'Range Not Satisfiable',
    category: 'client-error',
    description: 'The range specified by the Range header field in the request cannot be fulfilled.',
    whenToUse: ['When a client asks for a byte range outside the file bounds.'],
    whenNotToUse: ['For API pagination issues.'],
    exampleScenario: 'Client requests bytes 1000-2000 of a file that is only 500 bytes long.',
    developerMessage: 'Requested range not satisfiable. File size is 500 bytes.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 417,
    name: 'Expectation Failed',
    category: 'client-error',
    description: 'This response code means the expectation indicated by the Expect request header field cannot be met by the server.',
    whenToUse: ['When a client sends Expect: 100-continue but the server will not accept the request based on the headers.'],
    whenNotToUse: ['General validation rules.'],
    exampleScenario: 'Client asks to send a huge file, server sees it\'s unauthenticated and rejects the expectation.',
    developerMessage: 'Expectation failed.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 418,
    name: 'I\'m a teapot',
    category: 'client-error',
    description: 'The server refuses the attempt to brew coffee with a teapot. Created as an April Fools\' joke in 1998 (RFC 2324).',
    whenToUse: ['As an Easter egg.'],
    whenNotToUse: ['In any serious production context.'],
    exampleScenario: 'An easter egg in a developer tool.',
    developerMessage: 'I am a teapot.',
    productionMessage: 'I am a teapot.',
    hasBody: true,
    common: false,
  },
  {
    code: 421,
    name: 'Misdirected Request',
    category: 'client-error',
    description: 'The request was directed at a server that is not able to produce a response. This can be sent by a server that is not configured to produce responses for the combination of scheme and authority that are included in the request URI.',
    whenToUse: ['Mostly used in HTTP/2 when connection coalescing fails.'],
    whenNotToUse: ['Application logic.'],
    exampleScenario: 'TLS certificate mismatch in HTTP/2 connection sharing.',
    developerMessage: 'Misdirected request.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 422,
    name: 'Unprocessable Content',
    category: 'client-error',
    description: 'The request was well-formed but was unable to be followed due to semantic errors.',
    whenToUse: ['When a JSON payload is valid JSON, but the data fails business validation (e.g. password too short, email invalid).'],
    whenNotToUse: ['When the JSON syntax is broken (use 400).', 'When the request conflicts with database state like a uniqueness constraint (use 409).'],
    exampleScenario: 'Submitting a registration form where the "age" field is -5.',
    developerMessage: 'Validation failed: "age" must be a positive integer.',
    productionMessage: 'Please correct the errors in the form.',
    responseExample: '{\n  "success": false,\n  "errors": {\n    "age": "Must be greater than 0"\n  }\n}',
    hasBody: true,
    common: true,
    relatedCodes: [400, 409],
  },
  {
    code: 423,
    name: 'Locked',
    category: 'client-error',
    description: 'The resource that is being accessed is locked (WebDAV).',
    whenToUse: ['Mostly WebDAV.'],
    whenNotToUse: ['Standard REST APIs (prefer 409 for conflicts).'],
    exampleScenario: 'Trying to edit a document that is currently locked by another user in WebDAV.',
    developerMessage: 'Resource is locked.',
    productionMessage: 'This resource is currently locked by another process.',
    hasBody: true,
    common: false,
  },
  {
    code: 424,
    name: 'Failed Dependency',
    category: 'client-error',
    description: 'The request failed due to failure of a previous request (WebDAV).',
    whenToUse: ['When a transaction involving multiple steps fails in the middle.'],
    whenNotToUse: ['General application errors.'],
    exampleScenario: 'A batch WebDAV operation where an action fails because the prerequisite action failed.',
    developerMessage: 'Failed due to dependency error.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 425,
    name: 'Too Early',
    category: 'client-error',
    description: 'Indicates that the server is unwilling to risk processing a request that might be replayed.',
    whenToUse: ['In TLS 1.3 Early Data (0-RTT) when the request modifies state.'],
    whenNotToUse: ['Application logic.'],
    exampleScenario: 'A POST request sent via 0-RTT is rejected to prevent a replay attack.',
    developerMessage: 'Request rejected to prevent replay. Please retry normally.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 426,
    name: 'Upgrade Required',
    category: 'client-error',
    description: 'The server refuses to perform the request using the current protocol but might be willing to do so after the client upgrades to a different protocol.',
    whenToUse: ['When forcing a client to upgrade to TLS/HTTPS or a newer HTTP version.'],
    whenNotToUse: ['When upgrading to WebSockets (use 101).'],
    exampleScenario: 'Client sends an HTTP/1.1 request but the server demands HTTP/2.',
    developerMessage: 'Upgrade to HTTP/2 required.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 428,
    name: 'Precondition Required',
    category: 'client-error',
    description: 'The origin server requires the request to be conditional. Intended to prevent the "lost update" problem.',
    whenToUse: ['When an API mandates that clients must use If-Match headers for updates to avoid overwriting changes.'],
    whenNotToUse: ['When the precondition simply fails (use 412).'],
    exampleScenario: 'Client tries to PUT a resource without an If-Match header, but the server requires optimistic locking.',
    developerMessage: 'Missing If-Match header for concurrency control.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 429,
    name: 'Too Many Requests',
    category: 'client-error',
    description: 'The user has sent too many requests in a given amount of time ("rate limiting").',
    whenToUse: ['When an API rate limit or quota has been exceeded. Usually includes a Retry-After header.'],
    whenNotToUse: ['When the server is generally overloaded and crashing for everyone (use 503).'],
    exampleScenario: 'A script makes 100 requests per second to a free API tier that allows 10.',
    developerMessage: 'Rate limit exceeded (10 req/s). Retry-After: 60s.',
    productionMessage: 'You are making requests too quickly. Please wait a moment.',
    hasBody: true,
    common: true,
    relatedCodes: [503],
  },
  {
    code: 431,
    name: 'Request Header Fields Too Large',
    category: 'client-error',
    description: 'The server is unwilling to process the request because its header fields are too large.',
    whenToUse: ['When a client sends massive cookies or extremely large custom headers.'],
    whenNotToUse: ['When the URL is too long (use 414).', 'When the body is too large (use 413).'],
    exampleScenario: 'A browser sends a request with 10MB of bloated cookies.',
    developerMessage: 'Headers exceed maximum allowed size.',
    productionMessage: 'Your request headers are too large. Try clearing your cookies.',
    hasBody: true,
    common: false,
  },
  {
    code: 451,
    name: 'Unavailable For Legal Reasons',
    category: 'client-error',
    description: 'The user agent requested a resource that cannot legally be provided, such as a web page censored by a government.',
    whenToUse: ['When blocking access to content due to DMCA takedowns, censorship, or geo-blocking for legal compliance.'],
    whenNotToUse: ['When content is just deleted by an admin (use 404 or 410).'],
    exampleScenario: 'A user in a specific country tries to access a video that is banned there.',
    developerMessage: 'Content blocked due to legal restrictions in the user\'s region.',
    productionMessage: 'This content is unavailable in your location for legal reasons.',
    hasBody: true,
    common: false,
  },

  // 5xx Server Errors
  {
    code: 500,
    name: 'Internal Server Error',
    category: 'server-error',
    description: 'The server has encountered a situation it does not know how to handle.',
    whenToUse: ['When an unhandled exception or crash occurs in your application code.'],
    whenNotToUse: ['When a dependent microservice fails (use 502/503).', 'When the client sends bad data (use 4xx).'],
    exampleScenario: 'A database query throws a syntax error inside the API server.',
    developerMessage: 'Database error: Column "firstName" does not exist in relation "users". StackTrace: ...',
    productionMessage: 'Something went wrong on our end. Please try again later.',
    hasBody: true,
    common: true,
    relatedCodes: [502, 503, 504],
  },
  {
    code: 501,
    name: 'Not Implemented',
    category: 'server-error',
    description: 'The request method is not supported by the server and cannot be handled.',
    whenToUse: ['When an API endpoint is planned but the logic isn\'t written yet.', 'When a client uses an unknown HTTP method.'],
    whenNotToUse: ['When a known method is intentionally disabled for a specific resource (use 405).'],
    exampleScenario: 'An API specification documents a new feature, but a developer tries to use it before it\'s released.',
    developerMessage: 'Endpoint under construction.',
    productionMessage: 'This feature is not yet available.',
    hasBody: true,
    common: true,
  },
  {
    code: 502,
    name: 'Bad Gateway',
    category: 'server-error',
    description: 'This error response means that the server, while working as a gateway to get a response needed to handle the request, got an invalid response.',
    whenToUse: ['Typically generated by reverse proxies (like Nginx, AWS ALB) when the upstream application server crashes, resets the connection, or returns garbage.'],
    whenNotToUse: ['Normally shouldn\'t be returned directly by application code.'],
    exampleScenario: 'Nginx tries to proxy a request to a Node.js process, but the Node.js process crashes mid-request.',
    developerMessage: 'Upstream server returned an invalid response or closed the connection.',
    productionMessage: 'Service temporarily unreachable.',
    hasBody: true,
    common: true,
  },
  {
    code: 503,
    name: 'Service Unavailable',
    category: 'server-error',
    description: 'The server is not ready to handle the request. Common causes are a server that is down for maintenance or that is overloaded.',
    whenToUse: ['When deliberately taking down an API for maintenance.', 'When failing health checks.', 'When a downstream service is down.'],
    whenNotToUse: ['For client rate limiting (use 429).'],
    exampleScenario: 'A planned database migration requires taking the API offline for 5 minutes.',
    developerMessage: 'Service in maintenance mode. Retry-After: 300.',
    productionMessage: 'We are currently undergoing maintenance. Please check back soon.',
    hasBody: true,
    common: true,
    relatedCodes: [429],
  },
  {
    code: 504,
    name: 'Gateway Timeout',
    category: 'server-error',
    description: 'This error response is given when the server is acting as a gateway and cannot get a response in time.',
    whenToUse: ['Generated by reverse proxies/load balancers when the upstream application server takes too long to respond.'],
    whenNotToUse: ['Should generally not be returned directly by application code.'],
    exampleScenario: 'A heavy database query causes an API endpoint to take 45 seconds, but the AWS API Gateway times out at 30 seconds.',
    developerMessage: 'Upstream server failed to respond within the configured timeout.',
    productionMessage: 'The request took too long to complete.',
    hasBody: true,
    common: true,
  },
  {
    code: 505,
    name: 'HTTP Version Not Supported',
    category: 'server-error',
    description: 'The HTTP version used in the request is not supported by the server.',
    whenToUse: ['When a client attempts to use a very old or unsupported HTTP protocol version.'],
    whenNotToUse: ['Application logic.'],
    exampleScenario: 'Client sends an HTTP/1.0 request to a modern server requiring HTTP/2.',
    developerMessage: 'HTTP/1.0 is not supported.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 506,
    name: 'Variant Also Negotiates',
    category: 'server-error',
    description: 'The server has an internal configuration error: the chosen variant resource is configured to engage in transparent content negotiation itself, and is therefore not a proper end point in the negotiation process.',
    whenToUse: ['Rarely, due to server misconfiguration in content negotiation.'],
    whenNotToUse: ['Application logic.'],
    exampleScenario: 'Server configuration loop in Apache mod_negotiation.',
    developerMessage: 'Content negotiation misconfiguration.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 507,
    name: 'Insufficient Storage',
    category: 'server-error',
    description: 'The method could not be performed on the resource because the server is unable to store the representation needed to successfully complete the request.',
    whenToUse: ['When a server runs out of disk space or database quota (often used in WebDAV).'],
    whenNotToUse: ['When the client sends a file that is too large (use 413).'],
    exampleScenario: 'An upload fails because the server\'s hard drive is 100% full.',
    developerMessage: 'Server out of disk space.',
    productionMessage: 'Unable to save data at this time.',
    hasBody: true,
    common: false,
  },
  {
    code: 508,
    name: 'Loop Detected',
    category: 'server-error',
    description: 'The server detected an infinite loop while processing the request (WebDAV).',
    whenToUse: ['When a WebDAV operation encounters a circular reference.'],
    whenNotToUse: ['Standard REST APIs.'],
    exampleScenario: 'A WebDAV directory contains a symlink to itself.',
    developerMessage: 'Infinite loop detected in resource links.',
    productionMessage: '',
    hasBody: true,
    common: false,
  },
  {
    code: 510,
    name: 'Not Extended',
    category: 'server-error',
    description: 'Further extensions to the request are required for the server to fulfill it.',
    whenToUse: ['HTTP Extension Framework (obsolete).'],
    whenNotToUse: ['Modern APIs.'],
    exampleScenario: 'Obsolete.',
    developerMessage: 'Obsolete.',
    productionMessage: '',
    hasBody: true,
    common: false,
    deprecated: true,
  },
  {
    code: 511,
    name: 'Network Authentication Required',
    category: 'server-error',
    description: 'Indicates that the client needs to authenticate to gain network access.',
    whenToUse: ['Captive portals (like public Wi-Fi login pages).'],
    whenNotToUse: ['Application-level authentication (use 401).'],
    exampleScenario: 'Connecting to a hotel Wi-Fi and trying to browse before accepting the terms.',
    developerMessage: 'Network login required.',
    productionMessage: 'Please sign in to the network to access the internet.',
    hasBody: true,
    common: false,
  }
];
